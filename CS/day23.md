## Connection Timeout

- 종단 간 연결(TCP 3 way handshake)하는 데 소요되는 최대 시간
- 이 시간을 넘기게 되면 연결할 수 없다고 판단하고 에러가 발생

## Read Timeout

- 연결된 종단 간 데이터를 주고 받을 때 소요되는 최대 시간
- 이 시간을 넘기게 되면 데이터를 받을 수 없는 것으로 판단하고 에러가 발생